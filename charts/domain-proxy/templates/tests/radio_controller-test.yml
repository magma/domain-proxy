{{- if .Values.radio_controller.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-rc-test"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: {{ .Release.Name }}-rc-test
      image: subfuzion/netcat
      imagePullPolicy: "Always"
      args:
        - "-zv"
        - "domain-proxy-radio-controller"
        - "50053"
  restartPolicy: Never
{{- end }}
